  <div>
    <h2>Restaurants List</h2>
  </div>

  <div >
    <ul>
      <% @index.each do |list| %>
        <li>
          <%= link_to list.name, restaurant_path(list) %> - <%= button_to "Delete", restaurant_path(list),method: :delete %>
        </li>
      <% end %>
    </ul>
  </div>


<div>
  <p>
    <%= link_to "Add new restaurant", new_restaurant_path %>
  </p>
</div>
